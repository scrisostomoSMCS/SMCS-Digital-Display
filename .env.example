# Environment variables template.
# Copy to `.env.local` and fill in real values when integrations are added.
# Phase 1 (public home page) requires no environment variables.

# --- Added in a later phase (do not use yet) ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# DATABASE_URL=
